# 会话历史记录

## 2026年04月03日 11:13:00
- **会话目的**: 添加 GLM 模型测速功能
- **完成任务**: 在使用量报告页面底部新增测速区块，支持多模型选择、手动添加模型、实时进度、结果汇总对比
- **关键决策**:
  - 使用 SSE 流式请求 `open.bigmodel.cn/api/paas/v4/chat/completions` 测速
  - 测量 TTFT（首Token时间）、总耗时、生成速度（字符/秒）
  - 关闭 Webview 后台继续测速，结果缓存下次打开显示
  - 预设 4 个模型 + 用户可手动添加自定义模型
- **技术栈**: VSCode Extension Webview, SSE Streaming, TypeScript
- **修改文件**: `src/speedTest.ts`（新增）、`src/webviewPanel.ts`（添加测速UI和消息处理）、`package.json`（添加模型配置项）

## 2026年04月03日 10:51:14
- **会话目的**: API Key 脱敏显示，默认密文，点击切换明文
- **完成任务**: 在 Webview 报告页面的工具栏中，API Key 默认中间用 `****` 遮掩，点击后切换显示完整 Key
- **关键决策**: 前端纯 JS toggle 实现，后端新增 `maskKey()` 函数保留前6后4位
- **技术栈**: VSCode Extension Webview, TypeScript
- **修改文件**: `src/webviewPanel.ts`（新增 maskKey 函数、工具栏点击切换、CSS hover 样式）

## 2026年04月02日 00:41:57
- **会话目的**: 修复 GLM API 请求返回空响应（HTTP 200, 0 bytes）的问题
- **完成任务**: 将 HTTP 客户端从原生 `fetch` 替换为 `axios` 库，解决阿里云 WAF 导致的空响应
- **关键决策**:
  - 参考 GitHub 类似项目 `sigmanor/vscode-chutes-quota` 的方案
  - axios 自动处理 gzip 解压、重定向、cookie 管理，能更好应对阿里云 WAF 挑战
- **技术栈**: VSCode Extension, TypeScript, axios, GLM BigModel API
- **修改文件**: `src/apiClient.ts`（重写 HTTP 客户端）、`package.json`（新增 axios 依赖）
